# Traffic Stop Lights Simulation
Simply open the `index.html` on a Chrome browser.

# Install
`$ npm install`

# Run in command line
`$ npm start`

# Run website
`$ npm run web`

# Run tests
`$ npm test`
